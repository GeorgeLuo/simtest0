# Task Record — Remove CLI config example (implementer)

## Summary
- Removed the unused `simeval.config.example.json` template and updated README to avoid referencing it.

## Actions
- Deleted `simeval.config.example.json`.
- Removed the template reference in `README.md`.

## Validation
- Not run (doc/file cleanup only).

## Done-ness Evaluation
- Config guidance now lives solely in the README + `simeval config` commands.
