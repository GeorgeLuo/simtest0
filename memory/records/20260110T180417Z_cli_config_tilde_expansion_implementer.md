# CLI config tilde expansion (implementer)

- Added `~` expansion for `--cli-config` and `SIMEVAL_CLI_CONFIG` paths.

Done-ness: CLI config paths now accept `~` while keeping defaults under `~/.simeval/config.json`.
