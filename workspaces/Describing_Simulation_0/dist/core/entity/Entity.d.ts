/**
 * The runtime identifier for a simulation entity.
 * Future implementations will encapsulate creation semantics,
 * but presently we model an entity as a numeric handle.
 */
export type Entity = number;
