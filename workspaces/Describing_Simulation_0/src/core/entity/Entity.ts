/**
 * Represents a unique handle for items that exist inside the simulation world.
 *
 * An entity has no intrinsic shape besides its identifier; components describe it.
 */
export type Entity = number;
